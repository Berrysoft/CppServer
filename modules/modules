# 文件格式：键名 库名 显示信息
# 注释开头用“#”
# 虽然可以注释下面这行，但是非常不建议这么做
file        file        查看文件（默认为主页）

# 以下的可以随意注释
cpu         cpu         CPU与进程信息
version     version     系统版本、时间与内存信息
disk        disk        硬盘信息
markdown    markdown    简单Markdown解释器（默认为说明文档）
